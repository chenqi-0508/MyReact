import React from 'react'
import Modal from './Modal/modal'

export default function Loading(props) {
    return (
        < div >
        {
            props.isLoading && < Modal > < /Modal>}
                < /div>
)
}
